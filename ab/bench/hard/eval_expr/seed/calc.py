def evaluate(expr):
    """Evaluate an integer arithmetic expression and return an int.

    Supports + - * / , parentheses, and unary minus. Standard precedence
    (* and / bind tighter than + and -). Operands are non-negative integers.
    Division truncates toward zero (so 7/2 == 3 and -7/2 == -3). Whitespace is
    ignored. Raise ValueError on any malformed input.
    """
    raise NotImplementedError
