def merge(intervals):
    """Merge overlapping or touching intervals.

    intervals: list of [start, end] pairs with start <= end, possibly unsorted.
    Intervals that overlap OR touch are merged ([1,4] and [4,5] -> [1,5]).
    Return a new sorted list of [start, end] pairs. Return [] for []. Must NOT
    mutate the input list or its intervals.
    """
    raise NotImplementedError
